<mvc:View xmlns:core="sap.ui.core" xmlns:mvc="sap.ui.core.mvc" xmlns="sap.m"
	controllerName="halo.sap.mm.RECIPECOST.pages.controller.Recipes">
	
	<Table id="recipeTable"
		inset="false"
		items="{
			path: '/RecipeSet',
			sorter: {
				path: 'Name'
			}
		}">
		<headerToolbar>
			<OverflowToolbar>
				<content>
					<Title text="{i18n>Recipe}" level="H2"/>
					<ToolbarSpacer />
						<Button
							icon="sap-icon://add"
							type="Transparent"
							tooltip="Add Recipe"
							press=".onAddRecipe"/>
					<SearchField liveChange=".onSearch" width="100%" />
				</content>
			</OverflowToolbar>
		</headerToolbar>
		<columns>
			<Column
				width="12em">
				<Text text="Image" />
			</Column>
			<Column valign="Center">
				<Text text="{i18n>Recipe}" />
			</Column>
			<Column
				minScreenWidth="Tablet"
				demandPopin="true">
				<Text text="{i18n>Group}" />
			</Column>
			<Column
				minScreenWidth="Tablet"
				demandPopin="true">
				<Text text="{i18n>Location}" />
			</Column>
			<Column
				minScreenWidth="Tablet"
				demandPopin="true">
				<Text text="{i18n>Quantity}" />
			</Column>
		</columns>
		<items>
			<ColumnListItem>
				<cells>
					<Image src="{/RecipePhotoSet(Werks='PPHS',RecipeID='051MBHHZ7JKRIABKKBNWTM',VersionID='0000')/$value}" width="120px" height="80px" press="onSelectImage"/>
					<!--<Image src="{viewData>/pictureUrl}/{RecipeID}" width="80px" />-->
					<ObjectIdentifier
						title="{Name}"
						text="{RecipeID}"/>
					<ObjectIdentifier
						title="{GroupTxt}"
						text=""/>	
					<ObjectIdentifier
						title="{LocationTxt}"
						text=""/>
					<ObjectIdentifier
						title="{Quantity}"
						text="Unit"/>
				</cells>
			</ColumnListItem>
		</items>
	</Table>
</mvc:View>